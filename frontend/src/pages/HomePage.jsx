export default function HomePage() {
  return (
    <div className="container py-5 text-center">
      <h1 className="fw-bold">📋 Todo Manager</h1>
      <p className="text-muted">
        Feature 1: Authentication (Register / Login / Profile / Avatar) is active.
        Other features (Todos, Chat, Admin) will be added in later feature packages.
      </p>
    </div>
  );
}
